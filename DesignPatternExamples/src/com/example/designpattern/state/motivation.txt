   class Light {
     private static final int ON  = 0;
     private static final int OFF = 1;
     private static final int SLEEP = 2;
     private int state;

     public Light() { state = OFF; }

     public void on_button() {
       if (state == ON) { state = SLEEP;  } 
       else if ( state ==SLEEP ){ state = ON; } 
       else { state = ON; }
     }

     public void off_button() {
       if (state == ON) { state = OFF; }
       else if (sata == SLEEP) { state = OFF; }
       else { /* do nothing */ }
     }
   }  
   
   
   // SLEEP 클래스를 추가하고, ON클래스를 바꾼다.
   // OFF,State 클래스를 건들지 않아도 된다.
